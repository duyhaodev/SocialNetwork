spring:
  application:
    name: follow-service
  data:
    mongodb:
      uri: mongodb://root:root@localhost:27017/follow-service?authSource=admin
server:
  port: 8084
  servlet:
    context-path: /follow
app:
  service:
    profile: http://localhost:8081/profile